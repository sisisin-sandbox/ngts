import assert = require('power-assert');
import {HelloController} from '../../src/controller';

describe('karma', () => {
  it('works!', () => {
    let hoge = <HelloController>{};
    assert(true);    
  });
});
